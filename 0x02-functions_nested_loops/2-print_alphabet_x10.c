#include <main.h>

/**
 * a program that writes a function that prints
 * 10 times the alphabet in lowercase, followed by 
 * a new line
 */
void print_alphabet_x10(void);
          char = N;
	  for (N="a"; N<=10; N++);
{
	   _putchar(N="letter");
	   
}

            _putchar("\n");

